# Calculate the number of species in each grid/assemblage/site

Calculate the number of species in each grid/assemblage/site

## Usage

``` r
assemblage_nspecies(
  df.TS.TE,
  df.occ,
  time.slice,
  round.digits = 1,
  species = "species",
  TS = "TS",
  TE = "TE",
  Max.age = "Max.age",
  Min.age = "Min.age",
  site = "site"
)
```

## Arguments

- site:
